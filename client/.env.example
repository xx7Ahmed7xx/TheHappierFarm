# Copy to `.env` or `.env.local` and adjust if your API is not on 5216.
# Vite dev server port — must be listed under Cors:AllowedOrigins in the API appsettings.
VITE_DEV_PORT=5173

# Where Vite proxies /api and /hubs (your ASP.NET `applicationUrl` http port).
VITE_API_ORIGIN=http://localhost:5216
