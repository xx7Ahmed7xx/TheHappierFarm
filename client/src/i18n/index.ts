export {
  getLocale,
  initI18n,
  onLocaleChange,
  setLocale,
  t,
} from './core';
export { formatDuration, formatGameTimer } from './formatDuration';
export type { Locale, MessageTree } from './types';
