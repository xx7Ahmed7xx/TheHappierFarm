namespace HappierFarm.Domain;

/// <summary>
/// Legacy placeholder — gameplay catalog ids and economy live in SQL
/// (<see cref="Entities.CropDefinition"/>, <see cref="Entities.AnimalDefinition"/>,
/// <see cref="Entities.FactoryDefinition"/>, <see cref="Entities.ResourceDefinition"/>).
/// Admin v2 edits those tables; the client reads them from the farm snapshot API.
/// </summary>
public static class GameCatalog
{
}
